package edu.sjsu.cs.ghost151;

/**
 * A <b>BoardRenderer</b> implementation is responsible for creating a String
 * representation of the Board suitable for printing to stdout.
 */
public interface BoardRenderer {
	public String render(Board board);
}
